
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Backend",
      "type": "shell",
      "command": "pdm run uvicorn app.main:app --reload",
      "options": {
        "cwd": "${workspaceFolder}/backend"
      }
    },
    {
      "label": "Run Frontend",
      "type": "shell",
      "command": "npm start",
      "options": {
        "cwd": "${workspaceFolder}/frontend"
      }
    }
  ]
}
